{
    "version": 2,
    "functions": {
        "api/*.py": {
            "runtime": "vercel-python@0.8.0"  // Verifique a versão mais recente disponível
        }
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/index.py"
        }
    ]
}
